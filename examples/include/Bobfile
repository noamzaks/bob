from bob.prelude import *

rule("echo hi > $out").build("dummy.txt")

targets = []


def foo(name: str):
    targets.append(name)


include("targets")

include("flat", chdir=False)

subbob("flat-subbob/Bobfile", chdir=False)

bar(targets)
